"""collajit — make digital art from large image collections.

Three art modes (photo mosaic, generative layout, freeform collage) all produce
:class:`~collajit.model.layer.Layer` objects that a single compositor renders to a
final image. The PySide6 editor in :mod:`collajit.ui` drives it interactively.
"""

__version__ = "0.1.0"
